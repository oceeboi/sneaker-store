import { Gender, MediaType, ProductType } from '@/types/shared/product';

export const PRODUCT_TYPE_OPTIONS = Object.values(ProductType);
export const GENDER_OPTIONS = Object.values(Gender);
export const MEDIA_TYPE_OPTIONS = Object.values(MediaType);
